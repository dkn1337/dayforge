[package]
name = "dayforge"
version = "1.0.0"
description = "DayForge desktop workspace"
authors = ["Karol"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "dayforge_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
